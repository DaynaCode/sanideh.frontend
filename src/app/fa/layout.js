export default async function Layout({ children }) {
  return (
    <html lang="fa">
      <body>{children}</body>
    </html>
  );
}
export const metadata = {
  title: "سانیده",
  description: "شرکت سانیده",
};
